//generating

function genGraph(setup) {
  setup = setup || {

  }
    sizeX=innerWidth-toolBarHeight;
    sizeY=innerHeight


  function xy() {
    return [Math.randInt(sizeX),Math.randInt(sizeY)];
  }
}

function genGraphRandom() {

}
